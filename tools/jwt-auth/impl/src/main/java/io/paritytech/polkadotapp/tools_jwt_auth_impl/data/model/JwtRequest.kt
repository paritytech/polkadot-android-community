package io.paritytech.polkadotapp.tools_jwt_auth_impl.data.model

/**
 * Empty marker body for `POST /api/v1/auth/token`. The server requires a JSON
 * object even though no fields are sent — Gson serializes this to `{}`.
 */
object JwtRequest
