<?xml version="1.0" encoding="utf-8"?>
<!-- Holds the originating screen in place while the prize fragment slides
     in / out over it, so the transition reads as a modal pop-up. -->
<translate xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="300"
    android:fromYDelta="0"
    android:toYDelta="0" />
