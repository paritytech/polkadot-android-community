# Gradle files
.gradle/
.kotlin/
build/

# Rust build outputs
target/

# Local configuration file (sdk path, etc)
local.properties
.DS_Store

# Log/OS Files
*.log

# Android Studio generated files and folders
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
output.json

# IntelliJ
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Keystore files
*.jks
*.keystore

# Google Services (e.g. APIs or Firebase)
google-services.json

# Android Profiling
*.hprof

.prettierrc

.mcp.json
.cursor/
.codeium/
.gemini/

docs/.local

node_modules
*.js
**/.papi/metadata/

# Secrets
token*
.env*
!.env.example
# Claude Code: share docs, skills, hooks, and settings; keep local-only state out
/.claude/*
!/.claude/docs/
!/.claude/skills/
!/.claude/hooks/
!/.claude/settings.json
!/.claude/plugins
/.claude/settings.local.json
/.claude/worktrees/
/.claude/PLAN.md

# Sentry Config File
sentry.properties

# Sync tooling, not part of this repo. The upstream repo is still on
# io.pcf.polkadotapp; this script applies the rename during each sync into this
# repo, so it lives in the sync working copy and must never be committed here.
developer-tools/rename-pcf-to-paritytech.sh

.serena/
