/**
 * Products Chat SDK
 *
 * UI components and React rendering are provided by @novasamatech/product-react-renderer.
 */

// Welcome Message (send-once via host localStorage flag)
export { sendWelcomeMessage } from './welcomeMessage';
