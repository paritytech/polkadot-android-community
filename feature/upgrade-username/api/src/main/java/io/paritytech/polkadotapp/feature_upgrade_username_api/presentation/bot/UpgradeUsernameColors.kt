package io.paritytech.polkadotapp.feature_upgrade_username_api.presentation.bot

import androidx.compose.ui.graphics.Color

internal object UpgradeUsernameColors {
    val cardBg = Color(0xFF292A5B)
    val cardBorder = Color.White.copy(alpha = 0.12f)
    val previewBlockBg = Color(0xFF413B9C).copy(alpha = 0.84f)
    val primaryText = Color.White
    val mutedText = Color.White.copy(alpha = 0.48f)
    val pillBackground = Color(0xFF35C759).copy(alpha = 0.12f)
    val pillBorder = Color(0xFF35C759)
}
